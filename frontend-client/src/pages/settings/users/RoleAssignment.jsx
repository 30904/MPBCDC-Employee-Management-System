import PlaceholderPage from '../../../components/PlaceholderPage.jsx';

export default function RoleAssignment() {
  return (
    <PlaceholderPage
      title="Role Assignment"
      subtitle="Assign roles to client portal users"
      endpoints={['Role assignment placeholder']}
    />
  );
}