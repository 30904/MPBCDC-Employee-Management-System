import PlaceholderPage from '../../../components/PlaceholderPage.jsx';

export default function LeaveAccrual() {
  return (
    <PlaceholderPage
      title="Leave Accrual"
      subtitle="Accrual rules and carry-forward settings"
      endpoints={['Leave accrual placeholder']}
    />
  );
}