import PlaceholderPage from '../../../components/PlaceholderPage.jsx';

export default function LeaveWorkflow() {
  return (
    <PlaceholderPage
      title="Leave Workflow"
      subtitle="Approval flow for leave requests"
      endpoints={['Leave workflow placeholder']}
    />
  );
}