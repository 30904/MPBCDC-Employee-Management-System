import PlaceholderPage from '../../../components/PlaceholderPage.jsx';

export default function Departments() {
  return (
    <PlaceholderPage
      title="Departments"
      subtitle="Master list for organizational departments"
      endpoints={['Department CRUD API placeholder']}
    />
  );
}