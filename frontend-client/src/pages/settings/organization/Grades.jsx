import PlaceholderPage from '../../../components/PlaceholderPage.jsx';

export default function Grades() {
  return (
    <PlaceholderPage
      title="Grades"
      subtitle="Master list for employee grades"
      endpoints={['Grade CRUD API placeholder']}
    />
  );
}