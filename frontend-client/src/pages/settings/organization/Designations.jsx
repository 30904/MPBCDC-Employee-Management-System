import PlaceholderPage from '../../../components/PlaceholderPage.jsx';

export default function Designations() {
  return (
    <PlaceholderPage
      title="Designations"
      subtitle="Master list for job titles and designations"
      endpoints={['Designation CRUD API placeholder']}
    />
  );
}