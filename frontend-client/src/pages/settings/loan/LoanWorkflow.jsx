import PlaceholderPage from '../../../components/PlaceholderPage.jsx';

export default function LoanWorkflow() {
  return (
    <PlaceholderPage
      title="Loan Workflow"
      subtitle="Approval flow for loan requests"
      endpoints={['Loan workflow placeholder']}
    />
  );
}