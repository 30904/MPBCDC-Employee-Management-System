import PlaceholderPage from '../../../components/PlaceholderPage.jsx';

export default function LeaveApprovalQueue() {
  return (
    <PlaceholderPage
      title="Leave Approval Queue"
      subtitle="Pending leave approvals and reviews"
      endpoints={['Leave approval queue placeholder']}
    />
  );
}