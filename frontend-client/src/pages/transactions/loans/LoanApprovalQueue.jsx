import PlaceholderPage from '../../../components/PlaceholderPage.jsx';

export default function LoanApprovalQueue() {
  return (
    <PlaceholderPage
      title="Loan Approval Queue"
      subtitle="Pending loan approvals and reviews"
      endpoints={['Loan approval queue placeholder']}
    />
  );
}